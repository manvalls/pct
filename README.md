# pct

```javascript
var pct = require('pct');

//...

fixedURI = pct.fix(URI);

encodedURI = pct.encode(URI);
decodedURI = pct.decode(URI);

encodedURIComponent = pct.encodeComponent(URIComponent);
decodedURIComponent = pct.decodeComponent(URIComponent);
```
